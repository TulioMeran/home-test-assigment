# home-test-assigment

1. run the command "npm install".
2. run the command "npm start".


NOTE: 

The project use a library called csv-parser to handler the csv file format reading.

The fs module to handler the file path.

